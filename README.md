# Visual Reducer — упрощение фото и видео через эффекты

Инструмент для **локальной обработки изображений и видео**, ориентированный на
**уменьшение визуальной сложности**: ограничение палитры, сглаживание, подавление деталей
и применение эффектов на базе OpenCV.

Проект подходит для:

* подготовки визуалов под минималистичный стиль
* постеризации и цветовой редукции
* стилизации видео под «плоский» или сглаженный вид
* экспериментов с алгоритмическими эффектами

> Это **не ИИ** и не генератор арта «с нуля».
> Инструмент **трансформирует входной контент**, а не создаёт изображения самостоятельно.

---

## Основные возможности

* 📷 Поддержка **изображений и видео**
* 🎨 Ограничение количества цветов (`n_colors`, включая режим без лимита)
* 🌫 Сглаживание и подавление мелких деталей
* 🎛 Набор эффектов (модульная система)
* 🧪 Пакетная обработка (несколько файлов + комбинации параметров)
* ⚙ Полностью локальная работа (OpenCV + FFmpeg)

---

## Архитектура проекта

* `main.py` — CLI-интерфейс и управление пайплайном
* `processor/`

  * `single_image.py` — обработка изображений
  * `video.py` — обработка видео
  * `effects/` — эффекты (каждый эффект — отдельный модуль)
* `utils/` — ввод параметров, логирование, UI-диалоги

> Эффекты — это **алгоритмические пайплайны**, а не художественные пресеты.

---

## ВАЖНО про эффекты

❗ **Интерактивные поднастройки внутри эффектов отключены.**

Эффекты:

* используют **значения по умолчанию**
* или параметры, переданные из кода

Если нужно изменить поведение эффекта —
**редактируйте соответствующий файл в `processor/effects/`**
(функция `apply_*`).

---

## Установка

1. Создайте виртуальное окружение:

```bash
python -m venv .venv
source .venv/bin/activate    # macOS / Linux
.venv\Scripts\activate       # Windows
```

2. Установите зависимости:

```bash
pip install -r requirements.txt
```

3. Установите FFmpeg (не через pip):

* Windows — скачать сборку и добавить `ffmpeg.exe` в PATH
* macOS — `brew install ffmpeg`
* Linux — `sudo apt install ffmpeg`

---

## Быстрый старт

### Обработка одного изображения

```bash
python main.py image.png
```

### Обработка нескольких файлов

```bash
python main.py file1.jpg file2.png video.mp4
```

CLI запросит:

* количество цветов
* масштаб
* уровень размытия
* номер эффекта

Для видео используется один набор параметров, чтобы не создавать множество файлов.

---

## Видео: кодеки и качество

* Используется FFmpeg (`libx264`)
* Параметры по умолчанию:
  `-crf 18 -preset fast` — высокая визуальная точность при умеренном размере

Логика:

1. Кадры → временный AVI (MJPG)
2. Финальное кодирование → MP4

### Настройка качества

* Меньше `crf` → лучше качество / больше размер
* Диапазон `18–23` — оптимальный баланс

---

## Отладка: частые ошибки

* **FFmpeg не найден**
  → Проверьте, что `ffmpeg` доступен из командной строки

* **`NameError: np is not defined`**
  → Добавьте `import numpy as np` в эффект

* **Ошибки OpenCV (например, `pointPolygonTest`)**
  → Проверьте типы данных (`int32`, `float32`)

* **Мерцание цветов в видео**
  → KMeans должен обучаться **один раз**, а не на каждом кадре

* **Медленная обработка видео**
  → уменьшите `scale`, `n_colors` или сложность эффекта

---

## Тестирование эффектов — рекомендации

Тестируйте эффекты на разных типах сцен:

1. Пейзажи (градиенты)
2. Портреты (кожа, тени)
3. Иллюстрации / аниме
4. Городские сцены (много деталей)
5. Градиенты / радуга
6. Ч/б изображения
7. Пересвет / недоэкспозиция

---

## requirements.txt

```txt
numpy>=1.23
opencv-python>=4.6.0
scikit-learn>=1.1
scikit-image>=0.19
Pillow>=9.0
psutil>=5.9
colorama>=0.4
tqdm>=4.64
```